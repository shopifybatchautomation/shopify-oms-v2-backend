// DEPRECATED / dead code: never mounted in server.js (mrpLabel feature was
// unused, and called external hardcoded URLs). Kept as an empty stub (no
// delete access in this environment) -- safe to delete manually.
const express = require('express');
module.exports = express.Router();
