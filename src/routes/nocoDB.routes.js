// DEPRECATED / dead code: never mounted in server.js (NocoDB integration was
// unused). Kept as an empty stub (no delete access in this environment) --
// safe to delete manually.
const express = require('express');
module.exports = express.Router();
