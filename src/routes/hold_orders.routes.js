// DEPRECATED: replaced by ./holdOrder.routes.js as part of the MVC rewrite.
// Kept as an empty stub (no delete access in this environment).
const express = require('express');
module.exports = express.Router();
