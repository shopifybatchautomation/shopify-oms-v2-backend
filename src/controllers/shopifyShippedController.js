// DEPRECATED: the "Shipped / All Orders" concept was dropped in the rewrite
// (Confirmed Orders is now the terminal state; not part of the new sidebar).
// Kept as an empty stub (no delete access in this environment).
module.exports = {};
